spring.application.name=husc_researchHub

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/husc_researchhub?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:#14425nqtNQT14425#}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Cloud Storage (Supabase S3)
app.s3.endpoint=${SUPABASE_URL:https://<project>.supabase.co/storage/v1/s3}
app.s3.region=${SUPABASE_REGION:ap-southeast-1}
app.s3.accessKey=${SUPABASE_ACCESS_KEY:none}
app.s3.secretKey=${SUPABASE_SECRET_KEY:none}
app.s3.bucket=${SUPABASE_BUCKET:husc-research}

# JWT Configuration
app.jwtSecret=${JWT_SECRET:9a02115a835ee03d5fb83cd8a46853336921519303c537745655fb4f84654e32}
app.jwtExpirationInMs=604800000

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:noreply.huscresearchhub@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vyhz kgdz iegg evij}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
